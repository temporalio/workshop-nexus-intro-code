[project]
name = "workshop-nexus-intro"
version = "0.1.0"
description = "Code for the Introduction to Temporal Nexus workshop (Replay 2026)"
requires-python = ">=3.14"
dependencies = [
    "temporalio>=1.14.1",
    "nexus-rpc>=1.1.0,<2",
]
